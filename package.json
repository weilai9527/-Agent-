{
  "name": "multi-agent-interview",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "terminal:utf8": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/use-utf8.ps1",
    "db:init:mysql": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/init-mysql-db.ps1",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:admin": "npm run dev --prefix admin",
    "dev:backend": "npm run dev --workspace backend",
    "dev:admin-backend": "npm run dev --prefix admin_backend",
    "build": "npm run build --workspace frontend",
    "build:admin": "npm run build --prefix admin",
    "preview": "npm run preview --workspace frontend",
    "preview:admin": "npm run preview --prefix admin",
    "start": "npm run start --workspace backend",
    "start:admin-backend": "npm run start --prefix admin_backend"
  }
}
