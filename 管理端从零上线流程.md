# 管理端从零上线流程与改进建议

基于「多 Agent 模拟面试系统」当前代码梳理。管理端由前端 `admin/`（React+Vite）与后端 `admin_backend/`（FastAPI）组成，默认端口分别为 **5174** 与 **3002**。

---

## 一、系统架构概览

```text
候选人端前端 frontend/（5173）  ─────────┐
                                        ▼
候选人端后端 backend/（3001） ──► 共享业务库（候选人生成：candidates/interviews/reports/……）
                                        ▲
管理端前端 admin/（5174） ─────────┐     │
                                 ▼      │
管理端后端 admin_backend/（3002） ─► 同一业务库 + 管理库（admin_users/admin_sessions/……）
```

关键点：

- 管理端后端与候选人端后端**读同一份业务数据**（数据库默认 `../backend/data/dev.sqlite`，可切 MySQL），但各自是**独立进程、独立认证**。
- 管理端使用独立的 `admin_session` HttpOnly Cookie 认证，角色分三种：**超级管理员 / 运营管理员 / 报告审核员**。
- 系统配置、管理员账号管理**仅限超级管理员**操作。

---

## 二、从零上线完整流程

### 阶段 0：前置依赖

```bash
node / npm（前端构建）
python3 + pip（后端 FastAPI）
MySQL（若生产使用）或其他库
```

### 阶段 1：安装依赖

```bash
npm install
python3 -m pip install -r backend/requirements.txt
python3 -m pip install -r admin_backend/requirements.txt
```

### 阶段 2：准备环境变量

```bash
cp backend/.env.example backend/.env
cp frontend/.env.example frontend/.env
cp admin_backend/.env.example admin_backend/.env
cp admin/.env.example admin/.env
```

候选端后端 `backend/.env` 按本机库与模型服务配置；管理端 `admin_backend/.env` 重点看：

| 变量 | 说明 |
|------|------|
| `DB_ENGINE` / `SQLITE_PATH` / `MYSQL_*` | 数据库。本地默认 sqlite；生产切 MySQL，且**必须与候选端后端指向同一个库** |
| `ADMIN_FRONTEND_ORIGIN` | 允许的管理端前端来源，默认放行 5174/5175/5176 |
| `ADMIN_BOOTSTRAP_EMAIL/PASSWORD/NAME/ROLE` | **首次启动的初始化管理员**，邮箱不存在时创建 |
| `ADMIN_SESSION_MAX_AGE` | 会话时长，默认 8 小时 |
| `ADMIN_COOKIE_SECURE` | 生产必须 `true` |

### 阶段 3：启动候选人端（业务数据来源）

```bash
npm run dev:backend   # 后端 3001（首次启动会建表并写入职业技能目录初始版本）
npm run dev:frontend  # 候选人端 5173
```

> 管理端读的是候选人端产生的数据，因此候选人端后端应先于管理端可用。

### 阶段 4：启动管理端

```bash
npm run dev:admin-backend  # 3002（启动时 ensure_admin_schema + bootstrap 管理员）
npm run dev:admin          # 5174
```

### 阶段 5：初始化管理员并登录

1. 首次启动 `admin_backend` 时，若 `ADMIN_BOOTSTRAP_EMAIL` 在库中不存在，则会自动创建该超级管理员。
2. 打开 `http://127.0.0.1:5174/` 用初始化邮箱/密码登录。
3. 建议登录后：**在“系统与权限 → 管理员”中创建正式管理员账号**，随后删除 `.env` 中的 `ADMIN_BOOTSTRAP_PASSWORD`，避免长期保留强特权引导账号。

### 阶段 6：管理端登录后建议的初始化配置清单（按顺序）

1. **系统与权限 → 系统配置**：配置文本评分模型（`QWEN_API_KEY` / `OPENAI_API_KEY`、模型、供应商顺序、超时、重试），并执行脱敏连通性检测。
2. **组织与学生**：维护“学院 → 专业 → 班级”组织树。
3. **用户注册**：批量导入学生学籍（候选人端凭“学号+姓名”登录）。
4. **填写简历设置**：维护候选人简历表单的学院/专业下拉项。
5. **岗位与能力（职业技能目录）**：新建草稿版本 → 上传 Excel → 发布版本（候选人端据此渲染岗位/能力）。
6. **AI 陪练角色**：核对陪练角色及使用情况。
7. **报告质检**：抽检/复核/驳回候选人生成的成长报告。

---

## 三、管理端功能模块（导航）

| 分组 | 页面 | 权限点 | 主要能力 |
|------|------|--------|----------|
| 工作台 | 工作台 Dashboard | - | 待复核报告数、进行中面试、低分报告、优先处理队列 |
| 工作台 | 训练记录 | `canViewInterviews` | 查看训练进度与 AI 陪练运行 |
| 工作台 | 报告质检 | `canViewReports` | 抽检/通过/驳回报告（reviewer 优先） |
| 学生成长 | 组织与学生 | `canManageCampus` | 学院/专业/班级树、导入/编辑学生 |
| 学生成长 | 用户注册 | `canManageCampus` | 批量导入学籍登录信息 |
| 学生成长 | 填写简历设置 | `canManageCampus` | 管理简历表单下拉项 |
| 学生成长 | 学生成长 | `canViewCandidates` | 查看学生训练记录与成长表现 |
| 训练内容 | 岗位与能力（目录） | `canViewCatalog` | 目录版本管理、草稿、Excel 导入/发布、建议审核合并 |
| 训练内容 | AI 陪练角色 | `canViewAgents` | 角色信息与使用情况 |
| 系统 | 连接日志 | `canViewConnectionLogs` | 排查千问 WebRTC 的 ICE/SDP/数据通道/音频链路 |
| 系统 | 系统与权限 | `canManageSettings` | 系统配置、管理员增删/启禁用、审计日志 |

三种角色默认边界：**超级管理员**全量；**运营管理员**可维护组织/注册/目录导入等；**报告审核员**仅查看与报告质检。

---

## 四、改进建议（按优先级）

### 高优先级

1. **双后端共享库耦合，链路复杂**
   管理端与候选端各起一个 FastAPI 服务（3001/3002），却共享同一业务库、分别维护建表与认证。一旦数据模型调整，两侧 `database.py` 需同步改。建议在适合后收敛为**单一业务后端 + 单一管理后端**（或统一由一套 Schema 管理），避免 DDL 分叉。

2. **初始化管理员仅靠 `.env`，无任何安全闭环**
   - 引导账号密码以明文放在 `.env`，误提交仓库即泄露。
   - 无“首次登录强制改密”能力；README 建议手动删 `ADMIN_BOOTSTRAP_PASSWORD` 但无 UI 支撑。
   - 建议：首次登录强制改密 + 引导页关闭引导账号，并用密钥管理（如环境注入而非明文）。

3. **无登录重试/风控之外的自助恢复**
   - 已有 `MAX_LOGIN_ATTEMPTS=8` + 15 分钟窗口限流，但缺少“忘记密码”。README 明确当前**不交付密码重置**（无邮件服务）。
   - 建议至少在管理端提供“超级管理员手工重置他人密码”的能力，降低运维卡点。

4. **管理端认证缺少会话刷新与显式过期提示**
   - Cookie 会话固定 8 小时，无续期、无前端到期前提示，超时后请求静默失败。建议增加**滑动续期**与前端 401 统一跳登录。

### 中优先级

5. **“岗位与能力”依赖数据权限表 `catalog_role_permissions` 手动配置**
   角色能看/能导/能发目录依赖该配置；账号初始化时并不自动授予，容易初期“登录后看不到目录”。建议激活管理员时按角色**默认写入合理目录权限**，并支持 UI 调整。

6. **数据库模式切换体验差**
   默认 sqlite、生产切 MySQL 需手工改 `.env` 且两侧配置都要一致。建议提供统一的 `DB` 配置解读与启动自检，部署时一眼可确认连的是候选端同一实例。

7. **前端单个 `main.jsx` 过大（139KB+）**
   全部页面、抽屉、编辑器写在一个文件，不利于维护与按需加载。建议按路由分包拆分组件。

8. **初始化清单缺少“首次上线引导”**
   新人部署后面对 11 个导航页不知道该先做什么。建议管理端增加**首次登录向导/上线 checklist**，把第六阶段的操作顺序产品化。

### 低优先级

9. **报告质检与目录编辑器逻辑集中在单个巨文件**，建议拆分并补充组件级测试。
10. **连接日志仅为排查 WebRTC 链路**，可考虑扩展为通用操作审计，统一出入口。
11. **角色文案与权限点分散在 `navItems` 与 `roleLabels`**，建议集中为权限常量，便于扩展新角色。

---

## 五、本地验证命令速查

```bash
# 诊断模型供应商（不泄露密钥）
python -m backend.src.provider_diagnostics --config-only
python -m backend.src.provider_diagnostics

# 管理端后端测试
python -m pytest admin_backend/tests/
python -m pytest backend/tests/
```

---

*本文件由代码梳理整理，如功能有更新请同步维护。*