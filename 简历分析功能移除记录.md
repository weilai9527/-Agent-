# 简历分析页功能移除记录

> 记录时间：2026-09-20

## 背景

候选人端「简历分析」页面（顶部栏入口，组件 `ResumeAnalysisPage`）共有 6 张展示卡片被从前端**展示层**移除。移除原因：产品上暂时不需要在页面展示这些内容，但**后端能力与前端数据管线完整保留**，后续随时可恢复展示。

被移除的是**纯展示代码**，不涉及任何后端接口或数据结构改动。

> 说明：此前「结构化解析 / 简历亮点 / 风险点与短板 / 高概率面试题 / 简历优化建议」5 张卡片曾因文件回滚重新出现，本次已再次移除。

## 移除的功能清单（均为前端展示）

| # | 卡片标题 | 原所在区块 | 数据来源（`analysis` 对象字段） |
|---|---------|-----------|-------------------------------|
| 1 | 结构化解析 | `analysis-grid` | `analysis.parsedSections` |
| 2 | 简历亮点 | `analysis-grid` | `analysis.highlights` |
| 3 | 风险点与短板 | `analysis-grid` | `analysis.risks` |
| 4 | 高概率面试题 | `analysis-grid` | `analysis.questions` |
| 5 | 简历优化建议 | `resume-bottom-grid` | `analysis.suggestions` |
| 6 | 推荐面试配置 | `resume-bottom-grid` | `analysis.recommendedSetup` |

## 修改内容

文件：`frontend/src/main.jsx`

1. 删除 `analysis-grid` 区块（原含 4 张卡片：结构化解析、简历亮点、风险点与短板、高概率面试题）。
2. 删除 `resume-bottom-grid` 区块（原含 2 张卡片：简历优化建议、推荐面试配置；删除后区块整体移除）。
3. 删除仅被上述卡片使用的辅助组件 `InsightList`。
4. 移除 `ResumeAnalysisPage` 不再使用的 `onUseSetup` prop（含组件签名与父级调用处 `<ResumeAnalysisPage onUseSetup={...} />`）。
5. 页面顶部描述文案同步更新，不再提及「亮点风险、高概率追问、推荐面试配置」。

## 保留未动的部分

- 后端接口：`/api/profile/resume-analysis` 等分析相关接口均未改动。
- 前端数据管线：`buildResumeAnalysis`、`buildResumeAnalysisFromStructured`、`normalizeAnalysisList`、`buildBriefFromResumeAnalysis` 等仍照常生成 `parsedSections / highlights / risks / questions / suggestions / recommendedSetup` 字段，且 `handleAnalyze` 中仍读取 `recommendedSetup` 用于回填面试配置默认值，仅不再渲染。
- CSS：`.analysis-grid`、`.parsed-list`、`.insight-list`、`.insight-item`、`.recommended-setup`、`.resume-bottom-grid` 等样式仍保留在 `frontend/src/styles.css` 中，未删除（如需恢复可直接复用）。

## 恢复方法

如需恢复展示，按下面顺序操作即可：

1. 恢复 `ResumeAnalysisPage` 的 `onUseSetup` prop：

```jsx
function ResumeAnalysisPage({ onUseSetup }) {
```

并在父级调用处改回：

```jsx
{view === 'resume' && <ResumeAnalysisPage onUseSetup={() => setView('setup')} />}
```

2. 在 `ResumeAnalysisPage` 组件中，于「推荐就业方向」区块之后（`{!analyzed && ...}` 提示之前）重新加入 `analysis-grid` 区块：

```jsx
<section className="analysis-grid">
  <Card title="结构化解析" icon={<FileText size={18} />}>
    <div className="parsed-list">
      {analysis.parsedSections.map((item) => (
        <div key={item.label}>
          <strong>{item.label}</strong>
          <span>{item.value}</span>
        </div>
      ))}
    </div>
  </Card>

  <Card title="简历亮点" icon={<Award size={18} />}>
    <InsightList items={analysis.highlights} tone="green" />
  </Card>

  <Card title="风险点与短板" icon={<AlertTriangle size={18} />}>
    <InsightList items={analysis.risks} tone="amber" />
  </Card>

  <Card title="高概率面试题" icon={<MessageSquareText size={18} />}>
    <InsightList items={analysis.questions} tone="blue" />
  </Card>
</section>
```

3. 在 `analysis-grid` 区块之后重新加入 `resume-bottom-grid` 区块：

```jsx
<section className="resume-bottom-grid">
  <Card title="简历优化建议" icon={<Wrench size={18} />}>
    <InsightList items={analysis.suggestions} tone="blue" />
  </Card>

  <Card title="推荐面试配置" icon={<Layers3 size={18} />}>
    <div className="recommended-setup">
      {analysis.recommendedSetup.map((item) => (
        <div key={item.label}>
          <span>{item.label}</span>
          <strong>{item.value}</strong>
        </div>
      ))}
      <button className="primary-action" onClick={onUseSetup}>
        生成本场面试配置
      </button>
    </div>
  </Card>
</section>
```

4. 重新加入辅助组件（放在 `ResumeAnalysisPage` 组件定义之后）：

```jsx
function InsightList({ items, tone }) {
  return (
    <div className="insight-list">
      {items.map((item, index) => (
        <div className={`insight-item ${tone}`} key={item}>
          <span>{String(index + 1).padStart(2, '0')}</span>
          <p>{item}</p>
        </div>
      ))}
    </div>
  );
}
```

5. 如需一并恢复页面顶部描述文案，可改回：

```
先解析真实简历，再生成岗位匹配、亮点风险、高概率追问和推荐面试配置，让后续多 Agent 面试更贴近候选人经历。
```

说明：`FileText / Award / AlertTriangle / MessageSquareText / Wrench / Layers3` 图标在 `main.jsx` 顶部仍处于 import 状态（其他页面也在使用），恢复时无需额外添加 import。

---

# 能力画像页功能移除记录

> 记录时间：2026-09-20

## 背景

候选人端「能力画像」页面（顶部栏入口，组件 `StatsPage`）的「本周提升计划」卡片已从前端**展示层**移除。移除原因：产品上暂时不需要在页面展示该内容，但**后端能力与前端数据管线完整保留**，后续随时可恢复展示。

被移除的是**纯展示代码**，不涉及任何后端接口或数据结构改动。

## 移除的功能清单（均为前端展示）

| # | 卡片标题 | 原所在区块 | 数据来源 |
|---|---------|-----------|---------|
| 1 | 本周提升计划 | `stats-lower-grid` | `trainingTasks`（由画像维度的 `priorityDimensions / dimensions` 生成） |

## 修改内容

文件：`frontend/src/main.jsx`

1. 删除 `stats-lower-grid` 区块中的「本周提升计划」卡片（该区块仍保留「最近判断依据」卡片）。

## 保留未动的部分

- 后端接口：能力画像相关接口均未改动。
- 前端数据管线：`trainingTasks` 仍在生成，且「画像可信度」卡片中仍展示「提升建议 N 项」计数，仅不再渲染训练任务列表。
- CSS：`.training-plan-list`、`.training-step`、`.training-title` 等样式仍保留在 `frontend/src/styles.css` 中，未删除（如需恢复可直接复用）。

## 恢复方法

如需恢复展示，在 `frontend/src/main.jsx` 的 `StatsPage` 组件中，于「最近判断依据」卡片之前重新加入以下 JSX：

```jsx
<Card title="本周提升计划" icon={<Sparkles size={18} />} action={<StatusTag tone="amber">建议先练 1 项</StatusTag>}>
  <div className="training-plan-list">
    {trainingTasks.length === 0 ? (
      <div className="training-empty">
        <strong>等待生成个性化计划</strong>
        <span>完成一次面试后，系统会把短板转化为可执行训练任务。</span>
      </div>
    ) : trainingTasks.map((task, index) => (
      <article className={index === 0 ? 'priority' : ''} key={task.key}>
        <span className="training-step">0{index + 1}</span>
        <div>
          <div className="training-title">
            <strong>{task.title}</strong>
            <StatusTag tone={index === 0 ? 'amber' : 'blue'}>{task.score}/100</StatusTag>
          </div>
          <p>{task.text}</p>
          <small>{index === 0 ? '建议今天完成 · 约 15 分钟' : '完成上一项后解锁'}</small>
        </div>
      </article>
    ))}
  </div>
</Card>
```

说明：`Sparkles` 图标在 `main.jsx` 顶部仍处于 import 状态（其他页面也在使用），恢复时无需额外添加 import。
